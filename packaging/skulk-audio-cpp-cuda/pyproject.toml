[project]
name = "skulk-audio-cpp-cuda"
version = "0.8.2.post1"
description = "Pinned audio.cpp CUDA music server for Skulk on Linux"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "Apache-2.0" }
dependencies = []

[project.scripts]
skulk-audio-cpp-cuda = "skulk_audio_cpp_cuda:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/skulk_audio_cpp_cuda"]
artifacts = [
  "src/skulk_audio_cpp_cuda/bin/**",
  "src/skulk_audio_cpp_cuda/model_specs/**",
  "src/skulk_audio_cpp_cuda/licenses/**",
]

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"
